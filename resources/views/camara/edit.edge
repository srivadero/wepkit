@layout('layouts/main')

@section('content')
<h1>Edicion de camara</h1>
<form action="{{ route('camara.update', {'id': camara.id}) }}?_method=PUT" method="POST">
  {{ csrfField() }}

  <div class="form-group">
    <label>Nombre</label>
    <input type="text" class="form-control" name="nombre"
      value="{{ flashMessages.has('errors.nombre') ? flashMessages.get('nombre') : camara.nombre }}">
    @if(flashMessages.has('errors.nombre'))
    <small class="form-text text-muted">
      {{ flashMessages.get('errors.nombre') }}
    </small>
    @endif
  </div>

  <button type="submit" class="btn btn-primary">Guardar</button>
  <a href="{{ route('camara.index') }}" class="btn btn-link">Cancelar</a>
</form>

@endsection
